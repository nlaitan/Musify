import { Song } from './song';

export class Playlist{
    constructor(
        title: string,
        description: string,
        image: string,
        user: string,
        songs: Song[]
    ){ }
}